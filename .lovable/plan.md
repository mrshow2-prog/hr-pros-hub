I checked the uploaded files and the current code. The PDF was generated by the new `react-pdf` path, not the old canvas path, so the last fixes did run. The issue is that the rebuilt PDF component is still a separate simplified template, not a faithful export of the polished on-screen template. The Word file also confirms the current export is mostly paragraphs only: no tables, no real two-column structure, and only one document section.

Plan:

1. Rebuild PDF export as a proper designed document
- Replace the current simplified `CVPdfDocument` layout with a higher-fidelity template system.
- Fix the visible overlap in the header by separating name/title/contact into stable rows with enough height.
- Implement the Modern/two-column layout correctly on page 1: left/sidebar content and main body content with photo positioned intentionally.
- Force page 2+ into a clean single-column continuation layout with compact running headers.
- Prevent wasted language-only pages by keeping sidebar/footer sections on page 1 where possible, and flowing remaining small sections with education/skills instead of isolating them.
- Fix language formatting so empty levels never render as `()`.

2. Rebuild DOCX export with actual Word layout structures
- Use `docx` tables/sections/styles instead of plain paragraphs.
- Create real Word heading styles, font styles, colors, spacing, borders, and bullet numbering.
- Use a two-column table for the Modern first page so Word shows a designed layout, then continue with single-column content.
- Embed the photo at fixed dimensions without stretching.
- Apply template colors and section styling consistently.

3. Remove stale/unsafe export paths
- Remove the deprecated `exportNodeToPdf` fallback or make it impossible for Step 7 to use any canvas/plain placeholder route.
- Keep `html2canvas` unused by the CV builder export layer.

4. Verify with the uploaded Abanoub exports as the failure baseline
- After implementation, generate/inspect representative PDF and DOCX outputs from Abanoub-style data.
- For PDF: confirm text is selectable/vector, header doesn’t overlap, photo is positioned correctly, page 1 uses the intended layout, page 2+ continues cleanly, and no language-only page is produced.
- For DOCX: confirm there are real tables/styles/numbered bullets/image embedding and that the visual formatting survives conversion preview.

Technical notes:
- The uploaded PDF metadata says `Creator: react-pdf`, so the recent code path did execute.
- The current `CVPdfDocument` has a header layout bug: name/title/contact are packed too tightly, causing the title to overlap the name.
- The current DOCX has `tables 0`, one section, and only paragraph/list styling, which explains why it looks like an unformatted document.